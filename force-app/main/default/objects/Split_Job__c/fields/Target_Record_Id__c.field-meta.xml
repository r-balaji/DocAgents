<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Target_Record_Id__c</fullName>
    <description>Optional record (typically a Loan_Application__c) the split outputs should be attached to via ContentDocumentLink. Used by the headless flow when the portal creates a loan record and uploads a bundle in one shot.</description>
    <externalId>false</externalId>
    <inlineHelpText>Loan Application Id (or any record) the splits should appear on. Service creates a ContentDocumentLink per output.</inlineHelpText>
    <label>Target Record Id</label>
    <length>18</length>
    <required>false</required>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
